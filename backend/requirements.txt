# --- API server ---
fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4

# --- Open-source AI (compatibility scoring) ---
# Hugging Face sentence-embedding model, runs 100% locally (CPU is fine),
# no API key and no cloud calls required.
sentence-transformers==3.3.1
numpy==2.2.1
